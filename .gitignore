# Build output
dist/
.astro/
.vercel/

# Dependencies
node_modules/

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Private project docs / agent tooling — not for the public repo
/AGENTS.md
CLAUDE.md
backlog.md
learnings.md
changelog.md
scoreboard.md
measurement.md
research.md
.claude/
.agents/
skills-lock.json

# Local feature worktrees
.worktrees/

# Impeccable skill local state — not production code
.impeccable/

# Design handoff reference files — not production code, deleted after implementation
/design_handoff_og_images/
/design_handoff_changelog/

# Generated per-post OG images — rebuilt by scripts/generate-og-images.mjs
/public/og/

# Working plan directory — specs and build prompts, not production code
/steward/

# Superpowers skill working docs — plans/specs for in-progress features, not production code
/docs/superpowers/

# Steward (agents/steward) — local state only. `agents/steward/reviews/` is
# COMMITTED on purpose: the review archive is a dataset.
agents/steward/.temporal/
# Build-audit scratch: the Temporal dev-server DB, the worktree npm-ci lockfile
# hash, and Chrome user-data dirs. Regenerated on demand; none of it is data.
agents/steward/.cache/
agents/steward/.env
agents/steward/bin/
