---
import FilterPills from './FilterPills.astro';

interface Project {
  /**
   * The content collection's slug, which becomes the card's anchor.
   *
   * There are no per-project detail pages, so `/projects/#project-<id>` is the address a project
   * has. It is what llms.txt links each project bullet to (src/pages/llms.txt.ts), so an agent
   * reading the list has somewhere to follow — the anchor has to land on the card rather than at
   * the top of a page.
   */
  id: string;
  data: {
    title: string;
    description: string;
    tags: string[];
    github?: string;
    live?: string;
    status: 'live' | 'in-progress' | 'archived';
    date: Date;
  };
}

interface Props {
  projects: Project[];
}

const { projects } = Astro.props;

function formatDate(date: Date): string {
  const m = date.toLocaleDateString('en-US', { month: 'short', timeZone: 'UTC' }).toUpperCase();
  const y = date.getUTCFullYear();
  return `${m} ${y}`;
}

const STATUS_LABELS: Record<string, string> = {
  'live':        'LIVE',
  'in-progress': 'IN PROGRESS',
  'archived':    'ARCHIVED',
};

const pills = [
  { value: 'all', label: 'ALL' },
  { value: 'live', label: 'LIVE' },
  { value: 'in-progress', label: 'IN PROGRESS' },
  { value: 'archived', label: 'ARCHIVED' },
];
---

<div data-module="projects-wrapper">
  <FilterPills
    pills={pills}
    groupLabel="Filter by status"
    itemSelector='[data-module="projects-grid"] .project-item'
    itemDataAttr="status"
    matchMode="exact"
    noun="project"
    nounPlural="projects"
    filterVerb="with status"
  />

  <ol data-module="projects-grid" role="list">
    {projects.map(project => (
      <li class="project-item" data-status={project.data.status}>
        <article class={`project-card status--${project.data.status}`}>
          <header class="card-header">
            <div class="card-status">
              <span class="status-dot" aria-hidden="true"></span>
              <span class="status-label">{STATUS_LABELS[project.data.status]}</span>
            </div>
            <time datetime={project.data.date.toISOString()} class="card-date">
              {formatDate(project.data.date)}
            </time>
          </header>
          <h2 id={`project-${project.id}`}>{project.data.title}</h2>
          <p class="card-desc">{project.data.description}</p>
          <ul role="list" class="card-tags">
            {project.data.tags.map(tag => <li class="tag-pill">{tag}</li>)}
          </ul>
          <footer class="card-links">
            {project.data.github && (
              <a href={project.data.github} target="_blank" rel="noopener noreferrer" class="card-link">
                GITHUB ↗
              </a>
            )}
            {project.data.live && (
              <a href={project.data.live} target="_blank" rel="noopener noreferrer" class="card-link">
                LIVE ↗
              </a>
            )}
            {!project.data.github && !project.data.live && (
              <span class="no-link">No public link yet</span>
            )}
          </footer>
        </article>
      </li>
    ))}
  </ol>
</div>

<style>
  [data-module="projects-wrapper"] {
    max-width: var(--width-content);
    margin: 0 auto;
    padding: 0 var(--gutter) 72px;
  }

  [data-module="projects-grid"] {
    list-style: none;
    margin: 0;
    padding: 24px 0 0;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
  }

  .project-card {
    border: 1px solid var(--color-border);
    border-top-width: 2px;
    background: var(--color-card);
    padding: 20px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    height: 100%;
  }

  .status--live        { border-top-color: var(--color-status-green); }
  .status--in-progress { border-top-color: var(--color-accent); }
  .status--archived    { border-top-color: var(--color-label); }

  .card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .card-status { display: flex; align-items: center; gap: 6px; }

  .status-dot {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    display: inline-block;
    flex-shrink: 0;
  }

  .status--live        .status-dot { background: var(--color-status-green); }
  .status--in-progress .status-dot { background: var(--color-accent); }
  .status--archived    .status-dot { background: var(--color-label); }

  .status-label { font-family: var(--font-mono); font-size: 10px; letter-spacing: .14em; }
  .status--live        .status-label { color: var(--color-status-green); }
  .status--in-progress .status-label { color: var(--color-accent); }
  .status--archived    .status-label { color: var(--color-label); }

  .card-date { font-family: var(--font-mono); font-size: 10px; color: var(--color-label); }

  .project-card h2 {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: 19px;
    color: var(--color-heading);
    margin: 0;
  }

  .card-desc { font-size: 13px; color: #4a4436; line-height: 1.6; margin: 0; flex: 1; }

  .card-tags { list-style: none; display: flex; flex-wrap: wrap; gap: 6px; margin: 0; padding: 0; }

  .tag-pill {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--color-label);
    border: 1px solid var(--color-border);
    padding: 2px 8px;
    border-radius: 999px;
  }

  .card-links {
    display: flex;
    gap: 14px;
    padding-top: 10px;
    border-top: 1px solid var(--color-border);
  }

  .card-link {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--color-muted);
    text-decoration: none;
    transition: color .15s ease;
  }

  .card-link:hover { color: var(--color-accent); }

  .no-link { font-family: var(--font-mono); font-size: 11px; color: var(--color-label); }

  @media (max-width: 760px) { [data-module="projects-grid"] { grid-template-columns: 1fr; } }
  @media (max-width: 600px) { [data-module="projects-wrapper"] { padding: 8px var(--gutter-mobile) 56px; } }
</style>
